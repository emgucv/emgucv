PROJECT(Emgu.Util)

SET(CS_FLAGS ${CS_FLAGS} -doc:${LIBRARY_OUTPUT_PATH}/${PROJECT_NAME}.XML)
SET(SRC_UTIL *.cs)

#IF(WIN32)
#   ADD_CS_MODULE(${PROJECT_NAME} "${SRC_UTIL}")
#ELSE(WIN32)
   SET(SRC_UTIL $(SRC_UTIL) Properties/*.cs)
   ADD_CS_LIBRARY(${PROJECT_NAME} "${SRC_UTIL}")
#ENDIF(WIN32)

INSTALL(FILES ${LIBRARY_OUTPUT_PATH}/${PROJECT_NAME}.XML DESTINATION bin
COMPONENT binary)

