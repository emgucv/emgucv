IF(EMGU_CV_BUILD OR EMGU_CV_EXAMPLE_BUILD)

PROJECT(Emgu.Util)

SET(SRC_EMGU_UTIL *.cs Properties/*.cs)
GENERATE_DOCUMENT(${LIBRARY_OUTPUT_PATH}/${PROJECT_NAME})
SIGN_ASSEMBLY(Util.snk)
ADD_CS_LIBRARY(${PROJECT_NAME} "${SRC_EMGU_UTIL}")

INSTALL(FILES ${LIBRARY_OUTPUT_PATH}/${PROJECT_NAME}.XML DESTINATION bin
COMPONENT binary)

ENDIF()