# Generated by Set-Sqlite3Fallback's vcpkg manifest-mode installs (one
# separate install root per triplet -- see Install-VcpkgManifestPackage in
# ..\Build_Binary.ps1 for why they can't share a single root).
vcpkg_installed_*/
